apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  namespace: project
  name: broadcaster
spec:
  strategy:
    canary:
      steps:
        - setWeight: 100
        - analysis:
            templates:
              - templateName: staging-cpu-usage
  template:
    spec:
      containers:
        - name: broadcaster
          image: BROADCASTER/IMAGE
          env:
            - name: NOTIFICATIONS_DISABLED
              value: "1"
            - name: NATS_URL
              valueFrom:
                configMapKeyRef:
                  name: staging-configmap
                  key: NATS_URL
          resources:
            limits:
              cpu: "50m"
              memory: "64Mi"
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  namespace: project
  name: broadcaster-hpa
spec:
  scaleTargetRef:
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    name: staging-broadcaster
