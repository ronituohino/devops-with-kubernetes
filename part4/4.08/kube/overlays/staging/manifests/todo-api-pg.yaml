apiVersion: apps/v1
kind: StatefulSet
metadata:
  namespace: project
  name: postgres-stset
spec:
  template:
    spec:
      volumes:
        - name: pg-pvc-vol
          persistentVolumeClaim:
            claimName: staging-pg-pvc
